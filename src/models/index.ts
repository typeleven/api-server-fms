// @index: import ${variable} from ${relpath};
import accounts from './accounts';
import contacts from './contacts';
// /index

export default {
    // @index:${variable},
    accounts,
    contacts,
    // /index
};
