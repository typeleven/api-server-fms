// @index: import ${variable} from ${relpath};
import filemaker from './filemaker';
import validation from './validation';
// /index

export default {
    // @index:${variable},
    filemaker,
    validation,
    // /index
};
